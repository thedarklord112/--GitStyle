# ⚡ GitStyle (Conventional Commits Generator)


Every developer likes (or is required by their company) to keep their Git history organized using the Conventional Commits standard (e.g., `feat:`, `fix:`, `docs:`), but no one memorizes every single rule or which emoji matches each type of change.

What the app does: It provides an interactive dashboard where the developer selects the change type (e.g., "Bug Fix," "New Feature," "Performance"), types the text, and the app generates the perfect message using the industry-standard emoji (e.g., 🐛 `fix: clear memory leak crash`). A button copies the Git command line directly to the clipboard.

Why developers love it: It is a genuine productivity tool that streamlines anyone's workflow and makes the repository history look great.

---

## 🚀 Features
- 🏷️ **Conventional Standard Alignment:** Seamlessly structures headers based on standard `feat`, `fix`, `docs`, or `chore` guidelines.
- 🎨 **Visual Emoji Injection:** Automatically pairs matching developer emojis with corresponding code change scopes.
- 📋 **One-Click Clipboard:** Formats the entire string into a copyable `git commit -m "..."` shell execution command instantly.
